## Como usar los tests

1. Instala dependencias:

```bash
npm ci
```

2. Ejecuta la suite:

```bash
npm test
```

Si aparecen fallos, revisa el nombre del `describe` y del `test` para identificar el tema de ECMAScript que falta implementar en `homework.js`.
